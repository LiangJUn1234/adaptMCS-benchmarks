# Report Package Snapshot

This directory is a repository-local snapshot of the project report package
stored at `/home/lhftr/code/power-rare-events/report`.

Purpose:
- keep mentor-handoff and A4-prep materials inside the git repo;
- preserve the organized report structure without moving the original files;
- version the high-value markdown/csv/json/tsv report artifacts together with
  the code and experiment scripts.

Notes:
- The source `report/` directory remains the primary handoff workspace.
- This snapshot intentionally excludes unrelated presentation design binaries
  and other nonessential outer-project artifacts.
