function out = mp_run_acpf_minimal(case_data, options)
%MP_RUN_ACPF_MINIMAL Minimal AC power-flow wrapper placeholder (Phase 1).
%   out = MP_RUN_ACPF_MINIMAL(case_data, options) defines the future
%   MATLAB boundary for AC PF execution used by the AC extension.
%
%   TODO:
%   - Define input contract for case_data/options.
%   - Invoke MATPOWER runpf with stable options.
%   - Normalize outputs for Python adapter consumption.

error('Not implemented in Phase 1');
end
