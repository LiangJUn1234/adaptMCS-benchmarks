function out = mp_run_fdxb_minimal(case_data, options)
%MP_RUN_FDXB_MINIMAL Minimal FDXB solver wrapper placeholder (Phase 1).
%   out = MP_RUN_FDXB_MINIMAL(case_data, options) defines the future
%   MATLAB boundary for fast-decoupled PF execution in AC extension.
%
%   TODO:
%   - Define input contract for case_data/options.
%   - Invoke MATPOWER fast-decoupled PF path with fixed settings.
%   - Normalize outputs for Python adapter consumption.

error('Not implemented in Phase 1');
end
